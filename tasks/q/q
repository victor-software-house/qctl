#!/usr/bin/env bash
#MISE description="Run the qctl work-queue CLI"
#MISE dir="{{cwd}}"
#MISE raw_args=true
#MISE tools = { "github:victor-software-house/qctl" = "0.0.0" }

set -euo pipefail
exec "$(mise where github:victor-software-house/qctl)/qctl" "$@"
