#!/usr/bin/env bash
# Served: rendered from .ctl/templates/q.jinja onto the Version PR commit,
# which is the commit the tag names, so the tag installs itself. Edit the
# template, never this file.
#MISE description="Run the qctl work-queue CLI"
#MISE dir="{{cwd}}"
#MISE raw_args=true
#MISE tools = { "github:victor-software-house/qctl" = "0.2.1" }

set -euo pipefail
exec qctl "$@"
